/** Scroll Journey Extension — employment records rendered as connected career waypoints. */
import { experiences } from "@/content/loadContent";
import { SectionHeading } from "./SectionHeading";
export function ExperienceTimeline(){return <section className="section-shell experience-section journey-checkpoint" id="experience" data-journey="CAREER"><SectionHeading code="02" eyebrow="CAREER / WAYPOINTS" title="Each role marked a step forward." description="Follow the journey from software and embedded operations to medical robotics, mobile autonomy, AI, and production forklift navigation."/><div className="timeline">{experiences.map((e,index)=><article className={`experience-item mode-${e.mode}`} key={`${e.company}-${e.period}`}><div className="timeline-marker"><span>{String(index+1).padStart(2,"0")}</span></div><div className="experience-meta"><p>{e.period}</p><span>{e.location}</span></div><div className="experience-copy"><p className="eyebrow">WAYPOINT_{String(index+1).padStart(2,"0")} · {e.company}</p><h3>{e.role}</h3><h4>{e.project}</h4><p>{e.summary}</p><ul>{e.highlights.map(h=><li key={h}>{h}</li>)}</ul><div className="tag-row">{e.technologies.map(t=><span key={t}>{t}</span>)}</div></div></article>)}</div></section>}
