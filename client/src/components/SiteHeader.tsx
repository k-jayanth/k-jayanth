/** Dual-Mode Robotics Atlas — responsive command bar with visible brand mark and navigation. */
import { Menu, Moon, Sun, X } from "lucide-react";
import { useEffect,useState } from "react";
import { siteConfig } from "@/content/site";
const links=[["01","Profile","#profile"],["02","Experience","#experience"],["03","Projects","#projects"],["04","Capabilities","#capabilities"],["05","Contact","#contact"]];
export function SiteHeader(){const[open,setOpen]=useState(false);const[dark,setDark]=useState(true);useEffect(()=>{document.documentElement.classList.toggle("light-mode",!dark)},[dark]);return <header className="site-header"><a className="brand" href="#top" aria-label="Jayanth Kandregula home"><img src={siteConfig.logo} alt=""/><span><strong>JAYANTH KANDREGULA</strong><small>ROBOTICS ENGINEER</small></span></a><nav className={open?"nav-links is-open":"nav-links"} aria-label="Primary navigation">{links.map(([code,label,href])=><a key={href} href={href} onClick={()=>setOpen(false)}><small>{code}</small>{label}</a>)}</nav><div className="header-actions"><button className="icon-button" onClick={()=>setDark(v=>!v)} aria-label="Toggle light and dark theme">{dark?<Sun size={17}/>:<Moon size={17}/>}</button><a className="resume-link" href={siteConfig.resume} target="_blank" rel="noreferrer">Resume <span>↗</span></a><button className="menu-button" onClick={()=>setOpen(v=>!v)} aria-label="Toggle navigation" aria-expanded={open}>{open?<X/>:<Menu/>}</button></div></header>}
