/** Scroll Journey Extension — field reports presented as mission checkpoints along the route. */
import { ExternalLink } from "lucide-react";
import { projects } from "@/content/loadContent";
import { SectionHeading } from "./SectionHeading";
export function ProjectGrid(){return <section className="section-shell projects-section journey-checkpoint" id="projects" data-journey="PROJECTS"><SectionHeading code="03" eyebrow="SELECTED / MISSIONS" title="Robotic systems brought from concept toward reality." description="Industrial autonomy, exoskeletons, medical manipulators, perception pipelines, and applied natural-language intelligence."/><div className="project-grid">{projects.map((p,index)=><article data-code={`MISSION_${String(index+1).padStart(2,"0")}`} data-system={p.mode==="autonomy"?"AUTONOMY CHANNEL":"HUMAN-SYSTEM CHANNEL"} className={`${p.featured?"project-card featured":"project-card"} report-${p.mode}`} key={p.title}>{p.image?<div className="project-image-wrap"><img src={p.image} alt="" loading="lazy"/><span>WP_{String(index+1).padStart(2,"0")}</span></div>:<div className="project-signal" aria-hidden="true"><span/><span/><span/></div>}<div className="project-body"><div className="project-meta"><span>{p.type}</span><time>{p.period}</time></div><h3>{p.title}</h3><p>{p.summary}</p><p className="contribution"><strong>Contribution:</strong> {p.contribution}</p><div className="project-footer"><div className="tag-row">{p.technologies.map(t=><span key={t}>{t}</span>)}</div>{p.link&&<a href={p.link} target="_blank" rel="noreferrer" aria-label={`Open ${p.title} on GitHub`}><ExternalLink size={18}/></a>}</div></div></article>)}</div></section>}
