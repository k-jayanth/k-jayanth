/** Scroll Journey Extension — a control-stack checkpoint led by AI, MoveIt, Nav2, ROS 2, and humanoids. */
import { education, publication, skillGroups } from "@/content/site";
import { ExternalLink } from "lucide-react";
import { SectionHeading } from "./SectionHeading";
export function SkillsMatrix(){return <section className="section-shell capabilities-section journey-checkpoint" id="capabilities" data-journey="CONTROL STACK"><SectionHeading code="04" eyebrow="ROBOT / CONTROL STACK" title="Intelligence, planning, and motion in one stack." description="Primary interests in AI-enabled robotics, MoveIt manipulation, Nav2 autonomy, ROS 2 systems, and the emerging humanoid platform ecosystem."/><div className="skills-grid">{skillGroups.map(g=><article className="skill-group" key={g.code}><p>STACK_{g.code}</p><h3>{g.title}</h3><div className="skill-list">{g.skills.map(s=><span key={s}>{s}</span>)}</div></article>)}</div><div className="credentials-grid"><div className="education-panel"><p className="eyebrow">EDUCATION</p>{education.map(item=><div className="education-row" key={item.degree}><div><strong>{item.degree}</strong><span>{item.institution}</span></div><time>{item.year}</time></div>)}</div><a className="publication-panel" href={publication.link} target="_blank" rel="noreferrer"><p className="eyebrow">PUBLICATION / NLP</p><h3>{publication.title}</h3><span>{publication.venue} · {publication.year}</span><ExternalLink size={22}/></a></div></section>}
